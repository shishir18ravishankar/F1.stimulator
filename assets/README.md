# Assets

Drop the licensed royalty-free music file here as `the-game.mp3` and the
home page will play it automatically (muted autoplay, loop, speaker toggle
and volume slider in the top-right). Until the file exists, the page falls
back to an original Web Audio synth loop so the music control still works.
